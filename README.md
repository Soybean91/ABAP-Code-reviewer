The primary purpose of this report is to parse a specified Transport Request, identify ABAP program (PROG) objects and their associated includes, and package them into JSON format for consumption by an LLM
. The tool then retrieves review findings (specifically regarding coding standards and naming conventions) and displays them in a user-friendly ALV grid
